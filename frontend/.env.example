# Backend API base URL. Leave unset for local dev (defaults to
# http://localhost:8080). In production set this to the App Runner URL
# (terraform output backend_url) before running `npm run build`.
PUBLIC_API_URL=https://your-backend.awsapprunner.com
