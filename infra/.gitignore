# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log

# Variable files may hold secrets — commit an example instead if needed
*.tfvars
*.tfvars.json

# Override files are for local experiments
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# NOTE: .terraform.lock.hcl is intentionally NOT ignored — it pins provider
# versions and should be committed for reproducible builds.

# Lambda zip artifacts (archive_file output)
build/
